language: node_js

env:
  PORT="10010"

build:
  pre_ci_boot:
      image_name: node
      image_tag: 6
      pull: true

  ci:
    - shippable_retry npm install -q && npm cache clean
    - npm run-script lint
    - npm run-script ci:test
    - npm run-script ci:report